#!/usr/bin/env bash

# shellcheck source=/usr/bin/jh-lib
. jh-lib

export CR_WORKSPACE
CR_WORKSPACE="$(pwd)"

envsubst < /build/cryptomedic/website.conf > /etc/apache2/sites-enabled/website.conf

/usr/sbin/apache2ctl -DFOREGROUND -k start
