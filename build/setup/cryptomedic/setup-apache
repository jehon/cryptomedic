#!/usr/bin/env bash

set -o errexit

# shellcheck source=/usr/bin/jh-lib
. jh-lib

export DEBIAN_FRONTEND=noninteractive

# Enable modules
a2enmod expires rewrite \
    proxy proxy_http proxy_html proxy_connect proxy_wstunnel
