#!/usr/bin/env bash

# shellcheck source=/usr/bin/jh-lib
. jh-lib

export CR_WORKSPACE
CR_WORKSPACE="$(pwd)"

envsubst < build/website.conf > /etc/apache2/sites-enabled/website.conf

# apachectl restart
