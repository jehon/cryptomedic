; pour plus d'informations sur ce fichier:
; https://docs.ovh.com/fr/hosting/configurer-fichier-ovhconfig/

app.engine=php
environment=production
container.image=stable64

;
; Enabling the firewall made all post fail (2023-03-19)
;
http.firewall=none

# Dynamic
app.engine.version=$PHP_VERSION
