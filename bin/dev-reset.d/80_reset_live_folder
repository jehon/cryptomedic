#!/bin/bash

set -e

echo "* Reset the live folder from live-for-test"
rsync                         \
	--perms      			  \
    --times                   \
    --recursive               \
    --delete                  \
    --itemize-changes         \
    "$PRJ_DIR/live-for-test/" \
    "$PRJ_DIR/live/"
