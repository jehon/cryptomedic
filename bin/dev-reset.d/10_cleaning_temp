#!/bin/bash

set -e

sudo chown jehon.jehon -R $PRJ_DIR/target

if [ -d "$PRJ_DIR/target" ]; then
  find "$PRJ_DIR/target/" -mindepth 1 ! -name '.gitkeep' -delete
else
  mkdir -p "$PRJ_DIR/target"
fi

find . -name "*.log" -delete
