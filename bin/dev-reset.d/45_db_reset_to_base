#!/bin/bash

set -e

DBNAME=$( php "$PRJ_DIR"/config.php 'database.schema' )
BASE="$PRJ_DIR"/conf/database/base.sql

if [ ! -r "$BASE" ]; then
  echo "No '$BASE' base sql script found"
else
  echo "* Loading $BASE into $DBNAME"
  $MYSQL  --database="$DBNAME" < "$BASE"
fi
