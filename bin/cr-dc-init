#!/usr/bin/env bash

set -o errexit
set -o pipefail

# shellcheck source=./cr-lib
. "$(dirname "${BASH_SOURCE[0]}")"/cr-lib

header_begin "cr-dc-init"

"$CR_SCRIPT_DIR"/cr-mysql-wait

header_begin "cr-data-reset: cr-refresh-structure"
"$CR_SCRIPT_DIR"/cr-refresh-structure "http://localhost:5555/" "secret"
header_end

header_end
