#!/usr/bin/env bash

set -o errexit
set -o pipefail

# shellcheck source=./cr-lib
. "$(dirname "${BASH_SOURCE[0]}")"/cr-lib

BASE_HOST="$1"
TOKEN="$2"

if [ -z "$2" ]; then
    echo "$0 need parameters" >&2
    exit 1
fi

echo "Deploying to ${BASE_HOST} with pwd of #${#TOKEN} char length"

cr_curl_with_headers "Upgrading database" \
    "${BASE_HOST}/maintenance/patch_db.php?pwd=${TOKEN}"

cr_curl_with_headers "Upgrading files" \
    "${BASE_HOST}/maintenance/reset.php?pwd=${TOKEN}"
