#!/usr/bin/env bash

set -o errexit
set -o pipefail

# shellcheck source=./cr-lib
. "$(dirname "${BASH_SOURCE[0]}")"/cr-lib

OUTPUT="conf/database/base.sql"

mkdir -p "$(dirname "$OUTPUT")"

cr-mysqldump cryptomedic >"$OUTPUT"

echo
echo "Backup available at $OUTPUT"
